<%= form_with(model: user_order, local: true) do |form| %>
  <% if user_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_order.errors.count, "error") %> prohibited this user_order from being saved:</h2>

      <ul>
      <% user_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_order_user_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :user_order_product_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :user_order_quantity %>
  </div>

  <div class="field">
    <%= form.label :user_price_per_unit %>
    <%= form.text_field :user_price_per_unit, id: :user_order_user_price_per_unit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
